/**
 * Competitive Programming Snippet with test cases
 * 
 * In usual questions, the first input is tc (number of test cases)
 * and the first input in each test case is n (an integer).
 * 
 * The solution is usually entirely in the solve() function.
 */

#include <bits/stdc++.h>

#define fi(n) for (int i = 0; i < n; ++i)
#define fj(n) for (int j = 0; j < n; ++j)

using namespace std;
using ll = long long;
using vi = vector<int>;
using vvi = vector<vi>;

void solve() {
    int n;
    cin >> n;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int tc;
    cin >> tc;
    fi(tc) solve();
    return 0;
}