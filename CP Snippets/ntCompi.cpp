/**
 * Competitive Programming Snippet without test cases
 * 
 * In many questions, there is only 1 test case at a time,
 * and the first input in the program is n (an integer).
 * 
 * The solution is usually entirely in the solve() function.
 */

#include <bits/stdc++.h>

#define fi(n) for (int i = 0; i < n; ++i)
#define fj(n) for (int j = 0; j < n; ++j)

using namespace std;
using ll = long long;
using vi = vector<int>;
using vvi = vector<vi>;
using vpi = vector<pair<int, int>>;

void solve() {
    int n;
    cin >> n;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}